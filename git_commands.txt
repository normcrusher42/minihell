// lazygit shortcut // (change alias."cmp" to whatever you want the git alias to be. so like alias.lazygit makes 'git lazygit "COMMIT_MESSAGE"')
  
						  ^^^ change this to ur preferences

# ALWAYS git pull from origin everytime we start the day (assuming we've pushed and merged)
# And if we're switching devices to work on, ALWAYS git pull after cloning to avoid git conflicts
git pull origin main - pull the main
git checkout "branch_name" - switches branches
git lazygit "COMMIT_MESSAGE" - shortcut for pushing commits with message
